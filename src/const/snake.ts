// TOP && BOTTOM = j = 1
// LEFT && RIGHT = j = 1

export enum SnakeDirection {
    TOP = 'top',
    LEFT = 'left',
    RIGHT = 'right',
    BOTTOM = 'bottom',
}

export enum BoardElement {
    EMPTY,
    SNAKE,
    APPLE,
}